---
apiVersion: v1
kind: Service
metadata:
  namespace: netology-test-app
  name: test-app-svc
spec:
  selector:
    app: netology-test-app
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080